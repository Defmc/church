:load assets/nat.ac
:load assets/combs.ac

Fibo = ^n.(
    If (IsZero (Pred n)) 
        1 
        (Add 
            (Fibo (Pred n))
            (Fibo (Pred (Pred n)))
        )
    )

:gen_nats 0 4
:assert_eq (Fibo 3) 3
